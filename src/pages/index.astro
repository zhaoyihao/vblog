---
// 空的，只用来跳转
---

<script>
// 1. 旧域名 → 新域名
const allowedHost = "wdblog.cc.cd";
if (location.host !== allowedHost) {
  location.href = "https://" + allowedHost + location.pathname;
}

// 2. 自动进入你的博客主页（真正的首页）
if (location.pathname === "/") {
  location.href = "/blog";
}
</script>
