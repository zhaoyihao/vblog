---
// Props interface
interface Props {
  serverURL: string;
  lang?: string;
  dark?: string;
  emoji?: string[];
  meta?: string[];
  requiredMeta?: string[];
  reaction?: boolean;
  pageview?: boolean;
}

const {
  serverURL,
  lang = "zh-CN",
  dark = "html[data-theme-type='dark']",
  emoji = ["https://unpkg.com/@waline/emojis@1.1.0/weibo", "https://unpkg.com/@waline/emojis@1.1.0/bilibili"],
  requiredMeta = ["nick"],
  reaction = false,
  pageview = false,
} = Astro.props;
---

<div id="waline-container"></div>

<link rel="stylesheet" href="https://unpkg.com/@waline/client@v3/dist/waline.css" />

<script
  type="module"
  is:inline
  define:vars={{
    serverURL,
    lang,
    dark,
    emoji,
    requiredMeta,
    reaction,
    pageview,
  }}
>
  import { init } from "https://unpkg.com/@waline/client@v3/dist/waline.js";

  let walineInstance = null;

  // 销毁
  function destroyWaline() {
    try {
      if (walineInstance) {
        walineInstance.destroy();
        walineInstance = null;
      }
    } catch (e) {}
  }

  // 初始化
  function mountWaline() {
    destroyWaline();

    const container = document.querySelector("#waline-container");
    if (!container) return;

    container.innerHTML = "";

    setTimeout(() => {
      walineInstance = init({
        el: "#waline-container",
        serverURL,
        path: window.location.pathname,
        lang,
        dark,
        emoji,
        meta: ["nick", "mail"],
        requiredMeta: ["nick"],
        reaction,
        pageview,
        locale: {
          nick: '昵称（必填）',
          mail: '邮箱'
        }
      });
    }, 20);
  }

  document.addEventListener("astro:before-swap", destroyWaline);
  document.addEventListener("astro:page-load", mountWaline);

  if (document.readyState === "complete") {
    mountWaline();
  } else {
    window.addEventListener("load", mountWaline);
  }
</script>

<style>
#waline-container {
  margin-top: 2rem;
  margin-bottom: 2rem;
}

/* 100% 生效的粉红色输入框样式 */
#waline-container .wl-input input {
  border: 2px solid #ffb6c1 !important;
  border-radius: 8px !important;
  padding: 8px 12px !important;
  background: #fff !important;
}
#waline-container .wl-input input:focus {
  border-color: #ff8da1 !important;
  box-shadow: 0 0 0 2px #ffb6c166 !important;
  outline: none !important;
}
</style>
